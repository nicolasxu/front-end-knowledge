/*

	You need to render your layout view A first, then 
	'show()' the region within within the rendered 
	layout view A. 
	Or show() the region will show no effect, since
	the parent layout view does not exist and its region
	has no place to attach itself. 


 */

var explanation = "http://marionettejs.com/docs/v3.0.0-pre.2/marionette.layoutview.html#basic-usage";
