function number_to_hex_back() {

	var hexString = (11).toString(16); 
	// 'b' convert 11 in decimal to hex number

	var yourNumber = parseInt(hexString, 16); 
	// parse as hex, always return decimal

}